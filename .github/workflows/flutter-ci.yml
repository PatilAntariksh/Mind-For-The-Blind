name: Flutter CI/CD

on:
  push:
    branches:
      - main
      - function  # Runs on both branches
  pull_request:
    branches:
      - main
      - function

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.29.0'  # Update to the latest Flutter version

      - name: Install Dependencies
        run: flutter pub get

      - name: Run Analyzer
        run: flutter analyze

      - name: Run Tests
        run: flutter test

      - name: Build APK
        run: flutter build apk

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4 
        with:
          name: app-release.apk
          path: build/app/outputs/flutter-apk/app-release.apk
